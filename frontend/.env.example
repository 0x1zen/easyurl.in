# ── API ───────────────────────────────────────────────────────────────────────
# Base URL of the backend API — no trailing slash
# Development: point at your local Express server
# Production:  same origin as the frontend (nginx proxies /api/* to Express),
#              so this should be left empty or set to your domain
VITE_API_BASE_URL=http://localhost:3000
